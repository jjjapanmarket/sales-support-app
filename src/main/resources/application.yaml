spring:
  application:
    name: sales-support-app
  datasource:
    url: jdbc:mysql://localhost:3306/sales_management_db
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true